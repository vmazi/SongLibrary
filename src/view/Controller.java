package view;
/*Song Library
 * 
 * by Vamsi Kurkhal and Armin Grossrieder
 * 
 */

import java.util.Optional;

import app.Song;
import javafx.stage.Stage;

public class Controller {
	
	public void start(Stage stage){
		//create a List (that stores songs)
	}
	
	Stage home = new Stage();
	
	/* if a song is clicked on, then show all the info on the screen and 
	 * the ability to edit and delete (new stage!!)
	 */
	
	
	/*if user wants to CREATE, create event where
	 * Song song=new Song(title,artist,album,year);
	 * check if song already exists if not then insert at correect position (alphabetical)
	 * if song exists then tell the user to fuck off
	 */
	
	
	/*
	 * if user wants to EDIT a song, create event where
	 * song.setWhatever(newWhatever)
	 */
	
	
	/*
	 * if user wants to DELETE a song, algorithm to find and destroy the requested song
	 */
}
